import * as addConfig from "./add-config";
import * as getConfig from "./get-config";
import * as updateConfig from "./update-config";
import * as deleteConfig from "./delete-config";

export const serverQueries = {
    addConfig,
    getConfig,
    updateConfig,
    deleteConfig
};